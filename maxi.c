#include <stdio.h>
int main()
{
	int n,m;
	printf("ENTER TWO NUMBER TO CHECK WHICH NUMBER IS MAXIMUM=f");
	scanf("%d%d",&n,&m);
	if(n>m)
	printf("%d is maximum number",n);
	else
	printf("%d is maximum number",m);
}
